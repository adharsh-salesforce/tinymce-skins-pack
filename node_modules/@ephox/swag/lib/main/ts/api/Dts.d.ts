import { Plugin } from 'rollup';
import { Options } from 'rollup-plugin-dts';
import { CleanerOptions } from '../ast/ts/DtsCleaner';
interface DtsOptions extends CleanerOptions, Options {
    readonly clean?: boolean;
    readonly validate?: boolean;
}
declare const dts: (options?: DtsOptions) => Plugin;
export { DtsOptions, dts };
