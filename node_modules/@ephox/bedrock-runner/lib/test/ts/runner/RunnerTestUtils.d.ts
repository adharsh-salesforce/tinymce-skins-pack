import { HookType, LoggedError, Suite } from '@ephox/bedrock-common';
import { Reporter } from '../../../main/ts/reporter/Reporter';
import { RunState } from '../../../main/ts/runner/TestRun';
declare type LoggedError = LoggedError.LoggedError;
export interface MockReporter extends Reporter {
    readonly failures: () => LoggedError[];
}
export declare const TEST_TIMEOUT = 200;
export declare const populateHooks: (suite: Suite, count: number, func?: (idx: number, type: HookType) => void) => void;
export declare const MockReporter: () => MockReporter;
export declare const createRunState: (offset: number, chunk: number, count?: number) => RunState;
export {};
