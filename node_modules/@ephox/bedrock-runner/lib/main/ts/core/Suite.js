import { getFullTitle } from './Utils';
var create = function (title, root, parent) {
    var _a;
    var suite = {
        title: title,
        hooks: (_a = {},
            _a["before" /* Before */] = [],
            _a["beforeEach" /* BeforeEach */] = [],
            _a["after" /* After */] = [],
            _a["afterEach" /* AfterEach */] = [],
            _a),
        root: root,
        suites: [],
        tests: [],
        parent: parent,
        _only: false,
        _skip: false,
        isSkipped: function () {
            return suite._skip || (parent === null || parent === void 0 ? void 0 : parent.isSkipped()) === true;
        },
        fullTitle: function () { return getFullTitle(suite, '/'); }
    };
    return suite;
};
export var createSuite = function (title, parent) {
    return create(title, false, parent);
};
export var createRootSuite = function (title) {
    return create(title, true);
};
//# sourceMappingURL=Suite.js.map