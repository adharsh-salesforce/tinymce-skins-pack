/// <reference types="node" />
import { PluginOptions } from './Configuration';
import { Transform } from 'stream';
export interface Svg {
    name: string;
    data: string;
}
declare const iconPackager: (options: PluginOptions) => Transform;
export { iconPackager };
