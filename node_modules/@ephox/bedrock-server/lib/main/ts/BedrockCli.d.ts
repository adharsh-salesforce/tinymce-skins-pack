import { BedrockSettings } from './bedrock/core/Settings';
declare type Program = {
    go: <T extends BedrockSettings>(settings: T, directories: {
        current: string;
        bin: string;
    }) => void;
    mode: 'forAuto' | 'forManual';
};
export declare const run: (program: Program, directories: {
    current: string;
    bin: string;
}) => void;
export {};
