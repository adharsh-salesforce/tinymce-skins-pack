import { Browser } from 'webdriverio';
import { Attempt } from '../core/Attempt';
import * as Controller from './Controller';
import { DriverMaster } from './DriverMaster';
import * as Routes from './Routes';
export interface Apis {
    readonly routers: Routes.Route[];
    readonly markLoaded: () => void;
    readonly enableHud: () => void;
    readonly awaitDone: () => Promise<Controller.TestResults>;
}
export declare const create: (master: DriverMaster | null, maybeDriver: Attempt<any, Browser<'async'>>, projectdir: string, basedir: string, stickyFirstSession: boolean, singleTimeout: number, overallTimeout: number, testfiles: string[], loglevel: 'simple' | 'advanced', resetMousePosition: boolean) => Apis;
