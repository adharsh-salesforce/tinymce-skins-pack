import { Browser } from 'webdriverio';
export interface MouseData {
    readonly type: 'move' | 'click' | 'down' | 'up';
    readonly selector: string;
}
export declare const executor: (driver: Browser<'async'>) => (data: MouseData) => Promise<void>;
