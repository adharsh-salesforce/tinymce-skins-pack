export interface ResolvingSettings {
    readonly bucket: number;
    readonly buckets: number;
    readonly testfiles: string[];
}
export declare const filterBuckets: (bucket: number, buckets: number, testfiles: string[]) => string[];
export declare const resolve: <T extends ResolvingSettings>(t: T) => T;
export declare const log: <T extends ResolvingSettings>(t: T) => void;
export declare const resolveAndLog: <T extends ResolvingSettings>(input: T) => T;
