"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.compile = void 0;
const fs = require("fs");
const Webpack = require("../compiler/Webpack");
const compile = (tsConfigFile, scratchDir, basedir, exitOnCompileError, files, coverage, polyfills) => {
    const getCompileFunc = () => {
        return Webpack.compile;
    };
    const generate = async () => {
        const compile = getCompileFunc();
        const compiledJsFilePath = await compile(tsConfigFile, scratchDir, basedir, exitOnCompileError, files, coverage, polyfills);
        return fs.readFileSync(compiledJsFilePath);
    };
    return {
        generate
    };
};
exports.compile = compile;
//# sourceMappingURL=Compiler.js.map