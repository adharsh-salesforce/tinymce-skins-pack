import { Attempt } from '../core/Attempt';
import { CommandLineOptions } from 'command-line-args';
import { ClOption } from './ClOptions';
export declare type CliError = {
    command: string;
    errors: string[];
    usage: string;
};
export declare const extract: (command: string, desc: string, definitions: ClOption[], argv: string[]) => Attempt<CliError, CommandLineOptions>;
