import * as cli from './Cli';
import { Attempt } from '../core/Attempt';
import { BedrockAutoSettings, BedrockManualSettings } from '../core/Settings';
export interface Directories {
    current: string;
    bin: string;
}
export declare const forAuto: (directories: Directories, argv?: string[]) => Attempt<cli.CliError, BedrockAutoSettings>;
export declare const forManual: (directories: Directories, argv?: string[]) => Attempt<cli.CliError, BedrockManualSettings>;
export declare const logAndExit: (errs: cli.CliError) => void;
