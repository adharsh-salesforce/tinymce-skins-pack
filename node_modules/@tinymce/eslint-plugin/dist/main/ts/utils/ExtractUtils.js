"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractMemberIdentifiers = exports.extractIdentifier = exports.extractModuleSpecifier = void 0;
const extractModuleSpecifier = (node) => {
    if (node.type === 'ImportDeclaration') {
        const moduleSource = node.source.value;
        if (typeof moduleSource === 'string') {
            return moduleSource;
        }
    }
    return '';
};
exports.extractModuleSpecifier = extractModuleSpecifier;
const extractIdentifier = (node) => {
    // Node
    if ((node === null || node === void 0 ? void 0 : node.type) === 'Identifier') {
        return node;
        // Node.DOCUMENT_POSITION_PRECEDING
    }
    else if ((node === null || node === void 0 ? void 0 : node.type) === 'MemberExpression') {
        return (0, exports.extractIdentifier)(node.object);
        // ...Node
    }
    else if ((node === null || node === void 0 ? void 0 : node.type) === 'SpreadElement') {
        return (0, exports.extractIdentifier)(node.argument);
    }
    return null;
};
exports.extractIdentifier = extractIdentifier;
const extractMemberIdentifiers = (member) => {
    const identifiers = [];
    // Expand left-hand side
    if (member.object.type === 'Identifier') {
        identifiers.push(member.object);
    }
    else if (member.object.type === 'MemberExpression') {
        identifiers.push(...(0, exports.extractMemberIdentifiers)(member.object));
    }
    // Expand right-hand side
    if (member.property.type === 'Identifier') {
        identifiers.push(member.property);
    }
    return identifiers;
};
exports.extractMemberIdentifiers = extractMemberIdentifiers;
//# sourceMappingURL=ExtractUtils.js.map